/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["imgs.search.brave.com","media.graphassets.com"],
  },
};

module.exports = nextConfig;
